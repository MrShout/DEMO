server:
  port: 8080

spring:
  datasource:
    username: qdm673522542
    password: rongtao@1949
    url: jdbc:mysql://qdm673522542.my3w.com:3306/qdm673522542_db?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.example.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug